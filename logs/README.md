# Logs

Runtime log output directory.

Log files generated by the application services are stored here when running in development mode. In Docker deployments, logs are managed via container stdout/stderr and Docker's logging driver.

This directory is gitignored for runtime content; the `.gitkeep` file ensures the directory exists in the repository.
